{% extends 'base.html.twig' %}

{% block title %}Edit Labels{% endblock %}

{% block body %}
    <h1>Edit Labels</h1>

    {{ include('labels/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('labels_index') }}">back to list</a>

    {{ include('labels/_delete_form.html.twig') }}
{% endblock %}
