<form method="post" action="{{ path('product_listing_delete', {'id': product_listing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_listing.id) }}">
    <button class="btn">Delete</button>
</form>
