{% extends 'base.html.twig' %}

{% block title %}ProductListing{% endblock %}

{% block body %}
    <h1>ProductListing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_listing.id }}</td>
            </tr>
            <tr>
                <th>ProductNumber</th>
                <td>{{ product_listing.ProductNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_listing_index') }}">back to list</a>

    <a href="{{ path('product_listing_edit', {'id': product_listing.id}) }}">edit</a>

    {{ include('product_listing/_delete_form.html.twig') }}
{% endblock %}
