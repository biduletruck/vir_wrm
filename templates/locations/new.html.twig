{% extends 'base.html.twig' %}

{% block title %}New Locations{% endblock %}

{% block body %}
    <h1>Create new Locations</h1>

    {{ include('locations/_form.html.twig') }}

    <a href="{{ path('locations_index') }}" class="btn btn-dark">back to list</a>
{% endblock %}
