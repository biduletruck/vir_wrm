{% extends 'base.html.twig' %}

{% block title %}Locations index{% endblock %}

{% block body %}
    <h1>Locations index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Allée</th>
                <th>Location</th>
                <th>FreePlace</th>
                <th>Agence</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for location in locations %}
            <tr>
                <td>{{ location.driveway }}</td>
                <td>{{ location.Location  }}</td>
                <td>{{ location.FreePlace ? 'Yes' : 'No' }}</td>
                <td>{{ location.agency }}</td>
                <td>
                    <a href="{{ path('locations_show', {'id': location.id}) }}">show</a>
                    <a href="{{ path('locations_edit', {'id': location.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('locations_new') }}">Create new</a>
{% endblock %}
