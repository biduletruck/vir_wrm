{% extends 'base.html.twig' %}

{% block title %}FamilyProduct{% endblock %}

{% block body %}
    <h1>FamilyProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ family_product.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('family_product_index') }}">back to list</a>

    <a href="{{ path('family_product_edit', {'id': family_product.id}) }}">edit</a>

    {{ include('family_product/_delete_form.html.twig') }}
{% endblock %}
