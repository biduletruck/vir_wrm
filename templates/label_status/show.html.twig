{% extends 'base.html.twig' %}

{% block title %}LabelStatus{% endblock %}

{% block body %}
    <h1>LabelStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ label_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ label_status.name }}</td>
            </tr>
            <tr>
                <th>Enable</th>
                <td>{{ label_status.Enable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('label_status_index') }}">back to list</a>

    <a href="{{ path('label_status_edit', {'id': label_status.id}) }}">edit</a>

    {{ include('label_status/_delete_form.html.twig') }}
{% endblock %}
