{% extends 'base.html.twig' %}

{% block title %}DetailOrderType{% endblock %}

{% block body %}
    <h1>DetailOrderType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail_order_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ detail_order_type.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_order_type_index') }}">back to list</a>

    <a href="{{ path('detail_order_type_edit', {'id': detail_order_type.id}) }}">edit</a>

    {{ include('detail_order_type/_delete_form.html.twig') }}
{% endblock %}
